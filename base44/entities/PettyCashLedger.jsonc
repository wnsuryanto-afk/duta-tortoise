{
  "name": "PettyCashLedger",
  "type": "object",
  "properties": {
    "entry_type": {
      "type": "string",
      "enum": [
        "top_up",
        "pemakaian",
        "penyesuaian"
      ],
      "description": "top_up = isi saldo, pemakaian = uang keluar, penyesuaian = koreksi rekonsiliasi"
    },
    "amount": {
      "type": "number",
      "description": "Nominal (positif). Top_up menambah saldo, pemakaian mengurangi."
    },
    "qty": {
      "type": "number",
      "description": "Jumlah unit (opsional)"
    },
    "harga_satuan": {
      "type": "number",
      "description": "Harga per unit (Rp)"
    },
    "balance_after": {
      "type": "number",
      "description": "Saldo kas kecil setelah transaksi ini"
    },
    "category": {
      "type": "string",
      "enum": [
        "obat",
        "vitamin",
        "pakan",
        "peralatan_kandang",
        "transportasi",
        "solar_bbm",
        "rokok",
        "konsumsi",
        "lainnya"
      ],
      "default": "lainnya",
      "description": "Kategori pemakaian"
    },
    "description": {
      "type": "string",
      "description": "Keterangan pemakaian / sumber top up"
    },
    "entry_date": {
      "type": "string",
      "format": "date",
      "description": "Tanggal transaksi"
    },
    "proof_photo": {
      "type": "string",
      "description": "URL foto nota/bukti"
    },
    "recorded_by_name": {
      "type": "string",
      "description": "Nama yang mencatat"
    },
    "recorded_by_email": {
      "type": "string"
    },
    "recorded_by_role": {
      "type": "string",
      "description": "Role pencatat: admin/owner/manajer"
    },
    "finance_tx_id": {
      "type": "string",
      "description": "ID FinanceTransaction yang dibuat (hanya untuk pemakaian)"
    },
    "linked_request_id": {
      "type": "string",
      "description": "ID PettyCashRequest jika pemakaian berasal dari request yang disetujui (cegah dobel hitung)"
    },
    "notes": {
      "type": "string"
    }
  },
  "required": [
    "entry_type",
    "amount",
    "entry_date",
    "recorded_by_name"
  ]
}