{
  "name": "User",
  "type": "object",
  "properties": {
    "role": {
      "type": "string",
      "enum": ["owner", "manajer", "admin", "kepala_feeder", "keeper", "investor"],
      "default": "keeper",
      "description": "Role pengguna dalam sistem"
    }
  }
}